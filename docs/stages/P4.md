# P4 · 连续批处理调度引擎与 varlen prefill 拼批 — 阶段笔记

> 状态：**代码 + CPU 正确性 + GPU bench 全部完成（2026-09-24）**
> 日期：2026-09-24
> 提交：`02d4014`（feat P4 核心）+ varlen 拼批增量（待提交）

## 本阶段结论

- P4 目标：从 P3 的静态批（padding 浪费 61.5%）升级为**迭代级连续批处理**，
  任意时刻可插入新请求、抢占恢复；prefill 从逐条串行重构为 **varlen 拼批**一次算完。
- **正确性（CPU fp32）**：4 项测试全绿（`tests/test_p4_correctness.py`）
  1. 单请求 prefill+decode 输出与 P1 golden 一致
  2. 多请求并发连续批输出正确
  3. chunked prefill（budget=512）分块正确
  4. 抢占后 recompute 恢复正确
- **吞吐验收（GPU bf16）**：**torch varlen 后端 2.84× ✅ 达标**（目标 ≥2×）
  - P4 连续批 46.49 tok/s vs P3 静态批 16.40 tok/s
  - 对比旧版逐条 prefill 仅 1.93× 未达标

## 关键数据

| 项 | 值 | 来源 |
| --- | --- | --- |
| CPU 正确性 | 4 项全绿 | tests/test_p4_correctness.py |
| P4 吞吐（torch varlen） | 46.49 tok/s | bench/results/p4_bench_throughput_torch.json |
| P3 静态批吞吐 | 16.40 tok/s | 同上 |
| **吞吐比例（torch）** | **2.84× ✅** | 同上 |
| P4 吞吐（flashinfer） | 25.32 tok/s | bench/results/p4_bench_throughput_flashinfer.json |
| 吞吐比例（flashinfer） | 1.07× ❌ | 同上 |
| P3 padding 浪费率 | 61.5%（128/1024/8192 混排） | 同上 |
| TPOT chunked_512 wall | 7688 ms | bench/results/p4_bench_tpot_flashinfer.json |
| TPOT non_chunked wall | 5099 ms | 同上 |
| 旧版逐条 prefill 吞吐 | 1.93×（未达标） | bench/results/p4_bench_throughput.json |

## 架构设计（grill-me 6 Branch 锁定）

### B1：engine/ 三层架构
- `sequence.py` / `scheduler.py` / `core.py` 三层分离
- `runner.py` 退化为纯执行器（模型加载 + KV cache + sampler）
- `EngineCore.step()` 同步单线程（async 留 P5）

### B2：迭代级调度 + 三态状态机 + 抢占 recompute
- Sequence 三态：WAITING → RUNNING → FINISHED
- 每次 step 重新调度，任意时刻可插入新请求
- 抢占：LIFO 选最新请求回退到 WAITING，从头 recompute

### B3：token budget + LIFO 抢占 + watermark
- 统一 token budget 记账（初始 2048）
- 抢占保留 watermark（预留 1 block 防死锁）

### B5：chunked prefill 动态 chunk + decode 优先 + 混合 mask
- chunk 大小受 `max_num_batched_tokens` 限制
- decode 优先调度（decode 廉价，prefill 让路）
- chunked prefill 混合 mask：query 是 prompt 后缀 [kv_len-q_len, kv_len)，右对齐 causal

## varlen prefill 拼批（核心优化）

### 动机
P4 初版 prefill 逐条执行（每条一次 model forward），8K prompt 串行导致 GPU 利用率低，
吞吐仅 1.93× 未达标。varlen 拼批把本 step 所有 prefill chunk 扁平拼接成一条序列，
一次 model forward 算完。

### 关键洞察
**varlen 拼批的收益主要来自 MLP + QKV proj 一次算完**（旧版 12 条请求 = 12 次完整 forward，
新版 1 次 forward 对 total_q 个 token 一次算完），而非 attention kernel 本身。
即使 `varlen_prefill_torch` 内部逐条循环算 attention，model 其他部分已拼批，收益已经足够。

### 两种后端
- **torch**（`varlen_prefill_torch`）：逐条 gather KV + 右对齐 causal attention（CPU 可测 oracle）
- **flashinfer**（`varlen_prefill_flashinfer`）：`BatchPrefillWithPagedKVCacheWrapper`，
  KV 以 tuple (k_cache, v_cache) 传入，NHD 布局与 P3 PagedKVCache 完全兼容

### flashinfer 反而慢的原因（待优化）
flashinfer 后端 1.07× 反而比 torch varlen 慢 1.84×。疑似原因：
- wrapper 每步重建 + `plan()` 开销在小 batch（12 请求）下盖过 kernel 收益
- vLLM 里 wrapper 复用，仅 batch 结构变化时 re-plan
- 优化方向：缓存 wrapper，按 qo_indptr 哈希判断是否需要 re-plan

## 文件清单

### 新增
| 文件 | 作用 | 实现机制 |
| --- | --- | --- |
| `nano_vllm/engine/sequence.py` | Sequence 数据类 | 三态状态机 WAITING/RUNNING/FINISHED + SamplingParams + block_table |
| `nano_vllm/engine/scheduler.py` | 迭代级调度器 | token budget 记账 + LIFO 抢占 + watermark + chunked prefill 切分 + decode 优先 |
| `nano_vllm/engine/core.py` | 引擎主循环 | step() → schedule → execute → update；批量 decode + varlen 拼批 prefill |
| `nano_vllm/attention/varlen_prefill.py` | varlen prefill 拼批 attention | torch oracle（逐条 gather KV）+ flashinfer（BatchPrefillWithPagedKVCacheWrapper）+ 统一入口 |
| `tests/test_p4_correctness.py` | P4 正确性测试 | 4 项：单请求/多请求/chunked/抢占恢复 |
| `bench/p4_bench.py` | P4 吞吐 + TPOT bench | P4 连续批 vs P3 静态批 + `--prefill-impl` 切后端 |

### 修改
| 文件 | 改动 | 机制 |
| --- | --- | --- |
| `nano_vllm/models/qwen2.py` | `_forward_paged` 加 varlen prefill 分支 + b>1 批量 decode | `metadata.qo_indptr is not None` 时调 `varlen_prefill_attention`；decode b>1 走批量 paged attention |
| `nano_vllm/attention/metadata.py` | 扩展 varlen prefill + 批量 decode 字段 | qo_indptr/paged_kv_indptr/paged_kv_indices/paged_kv_last_page_len + block_tables/seq_lens |
| `nano_vllm/model_executor/runner.py` | 加 `prefill_impl` 参数 | "torch" 或 "flashinfer"，传给 EngineCore |

## 踩坑记录

1. **Write 工具内容重复写入**：`varlen_prefill.py`、`core.py`、`bench/p4_bench.py` 都出现过
   Write 把文件内容写两遍导致语法错误（`from __future__` 出现在文件中间）。
   修复：截取前半部分或重新 Write 单份内容。

2. **抢占时 victim 未从 scheduled 列表移除**：Scheduler step 2 抢占后 victim 仍在
   scheduled 列表里，导致 token_budget 双重扣减。修复：抢占时从 scheduled 移除 victim +
   恢复 token_budget。

3. **flashinfer wrapper 每步重建开销**：每次 `_run_prefill_batched` 都 new wrapper + plan()，
   小 batch 下开销盖过 kernel 收益。待优化：复用 wrapper + 按结构哈希判断 re-plan。

4. **bench TPOT 测量缺陷**：`bench_tpot` 只测了 wall_ms，没真正测 TPOT p99。
   chunked_512 7688ms vs non_chunked 5099ms 是总墙钟，不是 decode 间隔。待修复。

## 后续

- **P4 验收已达标**（torch varlen 2.84×）
- flashinfer wrapper 复用优化（可选，torch 后端已够用）
- 手写 Triton prefill kernel + flashinfer 对拍（Branch 4 面试价值，推后）
- bench TPOT p99 真正测量修复
- P5：async + 多模态（路线下一阶段）