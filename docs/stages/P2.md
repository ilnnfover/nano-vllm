# P2 · KV Cache 与静态批（引入缓存机制）— 阶段笔记

> 状态：**全部验收达成（2026-09-21）**——KV cache + prefill/decode 分离 + 静态批，正确性双证据 + bench 4.8×
> 日期：2026-09-21

## 本阶段结论

- P2 目标：引入 KV cache 消灭 P1 的全量重算开销，prefill/decode 分离的两段式 attention，静态批 padding 拼批。
- **正确性三层证据**：
  1. **batch=1 P2 cache vs P1 eager 逐 token 完全一致**（0.5B CPU fp32，32 token）—— KV cache 不改变结果，只省计算
  2. **batch=3 generate_batch vs 逐个 generate 逐 token 完全一致**（3 条不同长度 prompt）—— padding mask + 每请求独立 position_ids 正确
  3. **测试套件固化 4/4**（test_p2_correctness.py：cache-vs-eager / batch-vs-serial 极端长度差 / HF 对照 / seq_len 越界防护）
- **性能提升（GPU 1.5B bf16，16 条请求 short+medium）**：
  - **KV cache vs eager（run_local 口径）**：medium（prompt=1024）decode TPOT **62.0→21.6ms = 2.87×**；short（prompt=128）1.04×（GPU 重算短序列太快）；整体 TPOT 2.00×、吞吐 2.22×
  - **静态批 vs 串行（batch_bench 口径，均走 cache）**：串行 42.2 tok/s → **batch=8 279.2（6.61×）→ batch=16 392.3（9.29×）**，显存 peak 8.18GB
  - batch=1: 0.96×（与串行等价，sanity 基线正确）
- **浪费率（P3/P4 动机证据）**：batch=16 时 padding 浪费 41.2%（8 条 short 128 被 pad 到 medium 1024）、KV 预分配浪费 15%；batch=1/8 平均 KV 浪费 50%。静态批"done 请求随批空转 + 短请求 pad 到最长"是 P4 continuous batching 的直接动机。
- CPU 对照（0.5B fp32）：eager 7861ms vs cache 6798ms（1.2×）vs batch=5 1653ms（4.8×）；bench 口径 decode-only TPOT eager 425.7ms vs cache 93.6ms（4.5×）。

## 关键数据
 
| 项 | 值 | 来源 |
| --- | --- | --- |
| batch=1 cache vs eager | 32 token 逐 token 一致 | 0.5B CPU fp32 |
| batch=3 vs 逐个 generate | 3 条 prompt 全部 match | 0.5B CPU fp32 |
| 测试套件 | 4/4（P2 正确性）+ 7/7（sampler）| tests/ |
| **GPU cache vs eager TPOT（medium）** | **62.0ms → 21.6ms = 2.87×** | run_local 1.5B bf16 |
| GPU cache vs eager TPOT（short）| 24.1ms → 23.2ms = 1.04×（短序列 GPU 重算太快）| run_local |
| GPU cache vs eager 整体 | TPOT 44.7→22.3ms（2.00×），吞吐 20.2→44.9 tok/s（2.22×）| run_local |
| **GPU batch=16 吞吐** | **392.3 tok/s（9.29× vs 串行 42.2）** | batch_bench 1.5B bf16 |
| GPU batch=8 吞吐 | 279.2 tok/s（6.61×） | batch_bench |
| GPU batch=1 sanity | 0.96×（与串行等价）| batch_bench |
| GPU 显存 peak | 8.18GB（batch=16 prefill 激活为大头）| batch_bench gpu_mem |
| padding_waste_rate（batch=16）| 41.2%（short 128 pad 到 medium 1024）| batch_bench stats |
| kv_waste_rate | 15%（batch=16，seq 1088/1280）| batch_bench stats |
| CPU decode-only TPOT | eager 425.7ms vs cache 93.6ms（**4.5×**）| run_local 口径 |

## 文件变更清单

### 新增代码（1 个）

| 文件 | 行数 | 作用 |
| --- | --- | --- |
| `nano_vllm/kv_cache.py` | 62 | 连续预分配 KV cache（故意浪费版） |

**`kv_cache.py` — 实现要点**：布局 `[num_layers, batch, max_seq_len, num_kv_heads, head_dim]`，一开始按 max_seq_len 预分配整块显存，不回收、不分块。`write(layer_idx, k, v, start_pos)` 把 `[b, num_kv_heads, s, head_dim]` transpose 成 `[b, s, num_kv_heads, head_dim]` 写入对应位置；`read(layer_idx, total_len)` 读出 `[b, num_kv_heads, total_len, head_dim]`。`waste_rate = 1 - seq_len/max_seq_len` 作为 P3 动机证据。

### 修改代码（3 个）

- `nano_vllm/models/qwen2.py`：Attention/DecoderLayer/Qwen2Model/Qwen2ForCausalLM 全链路加 `kv_cache`/`is_prefill`/`cache_seq_len`/`attn_mask`/`position_ids` 参数。
  - **Attention**：prefill 时 K/V 写入 cache 后用当前 K/V 做 `is_causal=True` attention；decode 时 K/V 写入 cache 后从 cache 读全部历史 K/V 做 `is_causal=False` attention（query 1 个，key total_len 个）。`attn_mask` 非 None 时传给 SDPA 并强制 `is_causal=False`。
  - **Qwen2Model**：`position_ids` 可选参数（None 时内部从 `cache_seq_len` 生成，保持 batch=1 兼容）；batch>1 时 `expand(b, -1)`。
- `nano_vllm/model_executor/runner.py`：
  - `generate(use_cache=True)` 走 P2 prefill+decode 路径，`use_cache=False` 走 P1 eager 路径（对拍保留）。
  - `_prefill`/`_decode` 两个内部方法封装 cache 读写。
  - `generate_batch`：padding 拼批 + 每请求独立 position_ids + valid_mask 维护 + done 状态。`_build_prefill_mask` 构造 `[b,1,s,s]` causal+padding mask，`_build_decode_mask` 构造 `[b,1,1,total]` padding mask。
- `bench/run_local.py`：NanoBackend 加 `use_cache` 参数接入 KV cache 路径；BACKENDS 注册 `nano`（cache）/`nano-eager`（P1 对照），两种后端共用同一计时代码。

### 补充新增（验证补齐轮）

| 文件 | 作用 |
| --- | --- |
| `tests/test_p2_correctness.py` | 4 项测试：cache-vs-eager / batch-vs-serial（含极端长度差）/ HF 对照 / max_seq_len 越界防护 |
| `nano_vllm/kv_cache.py` 越界防护 | write 时 `start_pos+s > max_seq_len` 直接 raise，杜绝静默写错位置 |
| `bench/batch_bench.py` | 静态批吞吐基准：batch=N 拼批 vs 逐条串行（`generate_batch` vs `generate`），吞吐加速比 + padding/kv 浪费率 + `match_serial` 字段落盘 json；`--max-seq-len` 缺省按数据自动推导（max(prompt_len)+max_new_tokens 向上取 256 倍数） |

## 完成标准核对

- [x] greedy 输出与 P1 完全一致（batch=1 cache vs eager + batch=3 vs 逐个 + **nano vs HF 双模型对照**，测试套件固化）
- [x] decode 单步耗时相对 P1 显著下降（CPU 4.5×；GPU medium 2.87× / 整体 2.00×）
- [x] 浪费率数字落盘（独立 json：batch_bench stats 字段，padding 41.2% / kv 15%）
- [x] 静态批 batch=8/16 吞吐 vs 串行（**6.61× / 9.29×**，bench/batch_bench.py）
- [x] P2 测试套件 4/4（固化脚本可复跑）
- [x] P2 阶段笔记

## 踩坑记录

1. **padding 请求的 position_ids**：初版用统一 `cache_seq_len=max_prompt`，短请求（"Hello world" len=2）的 decode position_id 从 5 开始而非 2，导致 RoPE 位置编码错误、输出分叉。修复：每请求独立维护 `seq_lens`，构造 `[b, 1]` 的 decode position_ids。
2. **attn_mask 维度**：初版 `full = causal[None,:,:] & attn[:,None,:].bool()` 得到 `[b,s,s]`，与 SDPA 要求的 `[b,1,s,s]` 不匹配。修复：`causal[None,None,:,:] & attn[:,None,None,:].bool()` → `[b,1,s,s]`。
3. **KVCache batch 维**：初版只有 batch=1（无 batch 维），`write` 用 `k[0].transpose(0,1)`。改成 batch 维后统一为 `k.transpose(1,2)`（`[b,num_kv_heads,s,head_dim]` → `[b,s,num_kv_heads,head_dim]`）。
4. **越界静默写错**：初版 KVCache.write 无越界检查，序列超过 max_seq_len 时切片赋值静默截断（`cache[start:start+s]` 超出部分被丢弃），attention 读到错误 K/V 却不报错。修复：write 入口 raise。教训：**预分配 buffer 的切片写入必须显式校验边界**。
5. **数据长度恰好等于预分配容量**：medium 类别 prompt_len=1024 恰好等于 NanoRunner 默认 max_seq_len=1024——prefill 写满 1024 个位置后 decode 第一步 start_pos=1024 溢出 raise（踩坑 4 的 guard 正确拦截）。修复：batch_bench 按 `max(prompt_len)+max_new_tokens` 向上取 256 倍数自动计算，另加 `--max-seq-len` 覆盖。教训：**预分配容量必须从数据推导而非拍默认值**；guard raise 的报错信息（写入位置+容量）让根因一眼定位。
6. **GPU bf16 下 batch 与串行输出非逐 token 一致（定性为数值噪声非 bug）**：batch_bench 的 `match_serial=false`（batch=1/8/16 全部）。排查链：CPU fp32 与 CPU bf16 下 additive-mask 路径 vs `is_causal=True` 路径均逐 token 一致 → 逻辑无 bug；GPU 上 SDPA 依据"有无 attn_mask"选择不同 kernel backend（flash vs mem-efficient），归约顺序不同 × bf16 低精度 → 个别步 argmax 翻转。与 P0 踩坑 3（HF cache vs 重算路径 fp32 差 1e-6 即翻转）同构，也是 vLLM 业界共识（batch shape 变化导致数值差异）。**结论：跨数值路径不做逐 token 一致性承诺，一致性判据由同路径 CPU fp32 测试套件锁定（11/11）。**

## 遗留问题 / 下一步

- P2 全部验收达成，无遗留
- P3 起点：PagedAttention（BlockPool free list / block_table / slot_mapping）+ `reshape_and_cache` 写入核 + 自研 Triton paged attention kernel，把 KV 浪费率从 15% 降到 <2%；padding 浪费 41.2% 与 done 空转留给 P4 continuous batching